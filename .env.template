# =============================================================================
# ⚠️ 重要注意事项 / IMPORTANT NOTES
# =============================================================================
# 1. 值两边【千万不要】加引号 (DO NOT use quotes).
#    正确示例: OPENAI_BASE_URL=https://api.com
#    错误示例: OPENAI_BASE_URL="https://api.com"
#    原因: 当前是通过 Docker 的 --env-file 参数直接解析，引号会被当做值的一部分导致程序报错。
#
# 2. 无需手动加载 (Auto-loaded).
#    容器启动时会自动注入此文件中的变量，【不再需要】手动执行 `set -a; source .env; set +a;`。
# =============================================================================
AZURE_OPENAI_API_KEY=<YOUR_AZURE_OPENAI_API_KEY>
AZURE_OPENAI_ENDPOINT=<YOUR_AZURE_OPENAI_ENDPOINT>
OPENAI_API_VERSION=<YOUR_OPENAI_API_VERSION>

OPENAI_API_KEY=<YOUR_OPENAI_API_KEY>
OPENAI_BASE_URL=<YOUR_OPENAI_BASE_URL>

PATCH_DEBUG=0

# --- Proxy Settings ---